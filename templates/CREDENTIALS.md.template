# Credentials — {{PROJECT_NAME}}

> **⚠️ GITIGNORED — NEVER COMMIT THIS FILE.**
>
> API keys, connection strings, service accounts, and other secrets. This file is listed in `.gitignore` and must remain local. If a credential leaks into version control, rotate it immediately and update this file.

## Environments

### Development

- **Database**: (connection string or reference to local `.env`)
- **API keys** (per service): ...
- **Other services**: ...

### Staging

- **Database**: ...
- **API keys**: ...

### Production

- **Database**: ...
- **API keys**: ...

## External APIs

(For each external service you integrate with: endpoint, auth method, where the key/token lives.)

| Service | Endpoint | Auth | Secret location |
|---------|----------|------|-----------------|
| (Example) Stripe | `https://api.stripe.com/v1` | Bearer token | This file + Vercel env vars |

## Rotation Policy

(How often credentials are rotated, who is responsible, where the audit trail lives.)

## Emergency

If a credential is leaked:
1. Rotate the secret at the source immediately.
2. Update this file with the new value.
3. Invalidate any tokens or sessions tied to the old credential.
4. Note the incident (date, scope, who was notified) below.

### Incident Log

(none yet)
